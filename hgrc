[ui]
interface = curses

[extensions]
churn =
purge =
transplant =
strip =
shelve =

[merge-tools]
splice.executable = nvim
splice.args = -f $base $local $other $output -c 'SpliceInit'
splice.premerge = keep
splice.priority = 1

[hooks]
commit = ~/source/scripts/post_commit_phpcs.sh
pre-commit = ~/source/scripts/pre_commit_phpcbf.sh

[ui]
editor = ~/source/scripts/hg_verbose_commit.sh
