git:
  diffRenderers:
    - type: stdinFilter
      name: delta
      colorArg: always
      command: delta --dark --paging=never
  log:
    showGraph: always
    showWholeGraph: true
os:
  editPreset: nvim
